# ⚠️ IMPORTANT: install rdkit via conda, not pip
# conda install -c conda-forge rdkit

# Deep learning and ML
torch
torchvision
torchaudio
tensorflow==2.11.0
numpy==1.24.4
pandas
scikit-learn

# Image handling
pillow==9.5.0
matplotlib

# Web and frontend
flask
jupyterlab
# DECIMER core
DECIMER


# DECIMER image classifier (install manually)
pip install git+https://github.com/Iagea/DECIMER-Image-Classifier
# If pystow fails to download, manually dowload the pretrained from this url
# https://zenodo.org/record/8300489/files/models.zip
# and place it into the directory pystow is trying to download to
